//wap for student is accept or not accept seat in class check by roll no....
#include<stdio.h>
void main()
{
    int roll_no;
    printf("enter the roll no :");
    scanf("%d",&roll_no);
    roll_no<=50 ? printf("accepted ") : printf("not accepted");
}